{
  "name": "car-hire-app",
  "private": true,
  "version": "0.0.0",
  "description": "An internal AngularJS app for car hiring",
  "repository": "",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server src/main/webapp -a localhost -p 8000 -c-1 --proxy http://localhost:8080/car-hire"
  }
}